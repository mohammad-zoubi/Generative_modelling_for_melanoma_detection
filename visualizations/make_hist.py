''' Goal: make histogram given a csv file. Usefull for looking at number of biases '''

import os, sys
import pandas as pd

def n_bins(PATH_CSV): # input: csv path, output: array of n_bins
    df = pd.read_csv(PATH_CSV)


PATH_CSV = 
n_bins()